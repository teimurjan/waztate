{
  "name": "@waztate/store",
  "version": "0.1.0",
  "description": "Pure TypeScript implementation of waztate store",
  "author": "",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format=cjs,esm --dts",
    "dev": "tsup src/index.ts --format=cjs,esm --watch --dts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@waztate/types": "workspace:*"
  }
}
